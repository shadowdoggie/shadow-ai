/**
 * Shadow AI browser app entrypoint.
 *
 * The runtime is intentionally split across src/scripts/*.js and loaded in order by index.html.
 * Keep this file small so future work starts from the feature-specific files instead of a monolith.
 */

